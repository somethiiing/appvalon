const helpers = require('./helpers.js');
const enums = require('./enums.js');


/**
 * Creates an array of player objects with roles assigned
 */
const gameStart = (roomObj) => {
    helpers.setStatus(roomObj, enums.GameState. )
}