const assert = require('chai').assert;

const { createRoleAssignment } = require('../server/roleAssignment');

describe('test ', () => {
  it('should return true', () => {
    assert.equal(JSON.stringify({}), JSON.stringify({}));
  });
});